# INSTALL

- Last modified: Sun Dec 08, 2019  02:30AM
- Sign: JN

The C-code can be compiled using a standard C compiler.

For examle (using gcc v. 7.4.0):

    $ gcc -Wall -o splitfast splitfast.c

Alternatively, try the provided Makefile:

    $ cd /path/to/split-fasta-seq/src/
    $ make

Complete the installation by making sure the binary is placed in your PATH.
