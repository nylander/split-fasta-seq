# INSTALL

- Last modified: fre apr 18, 2025  02:30
- Sign: JN

The C-code can be compiled using a standard C compiler
and installed utilizing the make system.

For example

    $ cd /path/to/split-fasta-seq/src/
    $ make
    $ sudo make install

Or, if you have cmake:

    $ mkdir build
    $ cd build
    $ cmake ..
    $ make
    $ sudo make install
